[project]
name = "compute-sdk-python"
version = "0.1.0"
description = "Python SDK for Fastly Compute"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "art",
    "bottle",
    "componentize-py",
]
